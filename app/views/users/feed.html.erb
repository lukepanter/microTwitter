<p id="notice"><%= notice %></p>

<%id = params[:id]%>
<%@user = User.find_by(id: id)%>
<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">UTwitter</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href=<%= new_post_path(:id=>@user.id)%> >Create Post</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href= <%= edit_user_path(@user)%>>Edit profile</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href= "http://localhost:3000/main">Log out</a>
        </li>
      </ul>
    </div>
  </div>
</nav>

<p>
  <strong>Email:</strong>
  <%= @user.email %>
</p>

<p>
  <strong>Name:</strong>
  <%= @user.name %>
</p>
<!--<%= link_to 'CreatePost', new_post_path(:id=>@user.id) %>-->
<!-- <table>
  <thead>
    <tr>
      <th>msg</th>      
      <th>time</th>
      <th colspan="2"></th>
    </tr>
  </thead> -->
  <%pfeed = Array.new%>
  <%listOfFollower = @user.followees%>
  <%listOfFollower.each do |follower|%>
    <%tId = follower.follower_id %>
    <%tUser = User.find_by(id: tId)%>
    <%tUser.posts.each do |post|%>
      <%pfeed.push(post)%>
    <%end%>
  <%end%>
  <%@user.posts.each do |post|%>
    <%pfeed.push(post)%>
  <%end%>
  <%pfeed.sort! { |a,b| a.updated_at <=> b.updated_at }.reverse!%>
  <div>
    <% pfeed.each do |post| %>
      <div>
      	<div>--------------------------</div>
        <%id = post.user_id  %>
        <%user = User.find_by(id: id)%>
        <div><%=  user.name%></div>
        <div><%= post.msg %></div>
        <div><%= post.updated_at %></div>
        <div>--------------------------</div>
      <div>
    <% end %>
  </div>


<br>